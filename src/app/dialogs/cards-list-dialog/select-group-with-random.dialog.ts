import { Component } from '@angular/core';
import { SelectWithRandomDialog } from './select-with-random.dialog';

@Component({
    selector: 'app-select',
    templateUrl: './select-group-with-random.dialog.html',
    styleUrls: ['./select-dialog.css']
})
// tslint:disable-next-line:component-class-suffix
export class SelectGroupWithRandomDialog extends SelectWithRandomDialog { }
